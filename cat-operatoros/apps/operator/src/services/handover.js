/**
 * SHIFT HANDOVER — generated from live state, never templated text.
 *
 *   buildHandover(state) -> {
 *     machine, tasksCompleted, operatingTime, idleTime, safetyEvents,
 *     observations[], faultsLogged[], zoneNotes[], nextShift
 *   }
 *
 * This is where the "everything connects" claim is proved in one screen:
 * a hazard reported in minute two of the demo must appear here in minute
 * four, alongside the fault symbol logged on the Machine screen. Walk the
 * judges through that lineage explicitly.
 *
 * It is also the answer to "where is the supervisor view?" — the handover IS
 * the supervisor artefact. This product deliberately has no fleet dashboard.
 */
export function buildHandover(state) {
  // TODO(Block 11)
}
