/**
 * Hidden drawer of demo triggers plus [Play demo]. Opened by long-pressing
 * the header, so a judge never sees it by accident.
 */
export default function DemoControls() {
  // TODO(Block 12)
  return null
}
